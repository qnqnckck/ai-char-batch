spring:
  application:
    name: ${APPLICATION_NAME:AI-CHARGE-BATCH}
  datasource:
#    url: "jdbc:mysql://127.0.0.1:3306/soksok"
    url: "jdbc:mysql://soksok-db.cluster-cyr1npf6f1e2.ap-northeast-2.rds.amazonaws.com:3306/soksok_dev"
    username: "soksok_dev"
    password: "handle5565!!"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true

    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    properties:
      hibernate:
        show_sql: true
        format_sql: true
        jdbc:
          batch_size: 100


system:
  env:
    job_name: ${JOB_NAME}

