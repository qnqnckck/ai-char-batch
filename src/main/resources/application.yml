spring:
  application:
    name: ${APPLICATION_NAME:AI-CHARGE-BATCH}
  datasource:
    #url: "soksok-aurora-db-serverless.cluster-ctuthbmrxl9o.ap-northeast-2.rds.amazonaws.com"
    #      url: jdbc:mysql:aurora://<cluster endpoint where the writer instance is located>:<port>/<database name>?<optional parameters>
    url: "jdbc:mysql://127.0.0.1:3306/soksok"
    username: "soksok"
    password: "handle5565!!"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true